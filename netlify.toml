[build]
  command = "pnpm netlify:prepare && pnpm build"
  publish = "dist"
  edge_functions = ".netlify/edge-functions-build"

[dev]
  command = "pnpm dev"
  targetPort = 5173

[[edge_functions]]
  path = "/api/reddit/*"
  function = "reddit-proxy"
